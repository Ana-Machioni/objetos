/* a arrow function é mais concisa, utilizando => em vez d palavra
os parametros e o corpo da funcao sao os mesmos*/


// funçao de seta que soma dois numeros
const soma = (a, b) => a + b;

// funçao d seta que exibe uma saudacao
const saudacao = nome => console.log(`ola, ${nome}`);

//  funcao d seta que calcula o quadrado de um numero
const quadrado = x => x * x;

const areaQuadrado = lado => lado * lado

// funcao de seta que verifica se o numero é par 
const  ePar = num => num % 2 === 0;

// funcao de seta que exibe a data e hora atual 
const exibirDataHora = () => {
    const data = new Date() => {
}};